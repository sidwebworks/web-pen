import type { NextPage } from "next";
import { Editor } from "../components/playground/Editor";

const Home: NextPage = () => {
	return (
		<main>
			<Editor />
		</main>
	);
};

export default Home;
